version: '3.8'

services:
  frontend:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - NODE_ENV=development
      # Agrega aquí otras variables de entorno que necesites
      # - API_URL=http://backend:3001
